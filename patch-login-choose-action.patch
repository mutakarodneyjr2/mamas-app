--- src/pages/Login.tsx
+++ src/pages/Login.tsx
@@ -217,6 +217,7 @@
       
       setRecoveryToken(data.recoveryToken);
       setRecoveryRequestId(data.requestId);
+      setRecoveryOldPhone(data.phoneNumber);
       setSuccess('Code verified.');
       setStep('recovery-choose-action');
       
@@ -677,6 +678,28 @@
             </form>
           )}
 
+          {step === 'recovery-choose-action' && (
+            <div className="space-y-6 animate-in fade-in slide-in-from-right-4">
+              <p className="text-sm text-slate-600 mb-4 text-center">
+                Code verified. Choose how you want to recover your account:
+              </p>
+              <button
+                onClick={() => setStep('recovery-reset-pin')}
+                className="w-full flex justify-center py-4 px-4 rounded-xl shadow-sm text-sm font-bold text-mamas-primary bg-mamas-accent hover:bg-mamas-accent/90 transition-colors border border-mamas-primary/20"
+              >
+                Reset PIN (Keep {recoveryOldPhone})
+              </button>
+              <div className="relative my-6 text-center">
+                <span className="bg-white dark:bg-slate-800 px-4 text-xs font-bold text-slate-400 uppercase tracking-widest relative z-10">OR</span>
+                <div className="absolute top-1/2 left-0 right-0 h-px bg-slate-200 dark:bg-slate-700 -translate-y-1/2"></div>
+              </div>
+              <button
+                onClick={() => setStep('recovery-new-phone')}
+                className="w-full flex justify-center py-4 px-4 rounded-xl shadow-sm text-sm font-bold text-slate-700 dark:text-slate-200 bg-slate-100 dark:bg-slate-700 hover:bg-slate-200 transition-colors"
+              >
+                Link a New Phone Number
+              </button>
+            </div>
+          )}
+
           {step === 'recovery-new-phone' && (
             <form className="space-y-6 animate-in fade-in slide-in-from-right-4" onSubmit={handleSendRecoveryNewOtp}>
